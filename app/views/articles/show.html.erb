<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<%= link_to 'Back to Articles', articles_path, class: 'articles_index'%>

<%= link_to 'Delete', article_path(@article), method: :delete, data: {confirm: "Really delete this article?"} %>

<%= link_to 'Edit', edit_article_path(@article)%>
